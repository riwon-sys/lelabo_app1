name: lelabo_app1
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1               # HTTP 통신용
  image_picker: ^1.0.4       # 이미지 선택 기능
  cupertino_icons: ^1.0.8    # iOS용 아이콘

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/